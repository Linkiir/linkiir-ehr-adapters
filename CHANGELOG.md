# Changelog — Linkiir EHR Adapters

Every published change to this catalog, newest first. Library versions are immutable: a library fix appears here as a new version, never as an edit to an existing one.

## Unreleased

Initial content, migrated from the Linkiir authoring grid:

- Added **PCC Connect** (`LKEHR_PCC_CONNECT`) 1.0.0, source node.
- Added **PCC Request** (`LKEHR_PCC_REQUEST`) 1.0.0, transform node.
- Published library `pcc_api` 1.0.0.
